// 関数名は countCharacters とする。
// 引数は1つ取り、それはカウントしたい文字列(string)です。
// 返り値は、文字列中の各文字がキーで、その文字の出現回数が値となるオブジェクト(Record<string, number>)です。
// 大文字と小文字は区別しない（例: 'A'と'a'は同じ文字としてカウント）。
// 空白や句読点はカウントから除外する。
// countCharacters("Hello, World!")
// {
//   "h": 1,
//   "e": 1,
//   "l": 3,
//   "o": 2,
//   "w": 1,
//   "r": 1,
//   "d": 1
// }

export function countCharacters(characters:string){
    return characters
}